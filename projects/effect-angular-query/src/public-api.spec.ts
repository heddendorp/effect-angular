import * as PublicApi from './public-api';

describe('effect-angular-query public API', () => {
  it('exports EffectAngularQuery', () => {
    expect(PublicApi.EffectAngularQuery).toBeTruthy();
  });
});
