---
effect-angular-query: patch
---

Align generated client helper types with runtime nested path helpers for dotted RPC tags (for example `client.finance.receipts.my`), instead of exposing flattened dotted keys under a single prefix property.
