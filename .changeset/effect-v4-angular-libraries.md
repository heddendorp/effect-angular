---
effect-platform-angular: minor
effect-angular-query: minor
---

Migrate the Angular integrations to Effect v4 beta. Consumers should use `effect` v4 unstable HTTP/RPC imports instead of the old `@effect/platform` and `@effect/rpc` packages.
